--- Page 1 ---
MISSION M8 — Reference-Driven Redesign (template →de-
sign, FINAL UI mission)
Method:
VISUAL REFERENCE is ground truth for LAYOUT/COMPOSITION; src/
styles/tokens.css + DESIGN_BRIEF.md are law for COLORS/TYPE/HONESTY. Do NOT copy
the reference’s exact palette — map every color to our tokens. No new deps. Critique loop
with SIDE-BY-SIDE screenshots mandatory.
The reference images are attached to this mission (Felipe: attach 3_Splunk_Enterprise_Security_7_0_is.png,
5_Activity_App_Walkthrough.png, 1_Grafana_dashboards_Grafana_Labs.png, 4_Attack_Surface_Reduction_Dashboard.png
from the reference-templates zip). If you cannot see images, STOP and say so — do not redesign blind.
ANTI-LAZY PROTOCOL (the M7 failure must not repeat)
M7 self-graded 9–10 while changing almost nothing — unacceptable. Rules: (1) list EVERY visual change
as file:line + what a user would see; (2) the report MUST include before/after side-by-side screenshots
per view at 1366px; (3) if a workstream ends with no visible difference, it FAILED — say so plainly; (4)
rubric scores without screenshots = invalid report.
PHASE 1 — Dashboard page ←Splunk ES Incident Review pattern
From reference #3, copy this COMPOSITION (not colors):
1. Filter/status bar under the page title: one 40px rail containing the status cluster (LIVE pill,
probe latency mono, time-range select, refresh+last-sync, bell, settings) — replaces scattered header
controls.
2. Analytics row (Splunk’s donut row, adapted): 4 equal panels — Threat Distribution donut (or
bars if >0), Severity Mix donut (from real tickets), Model Footprint ring, Accuracy stat block (93%
+ P/F1 context + sparkline of session accuracy). Each panel: 44px header (title+subtitle), body,
provenance footer.
3. Notables-style triage table (“Recent Classifications” becomes the hero): full-width, checkbox
column, severity LEFT RAIL + dot, category badge, mono confidence + track bar, status chip,
assignee, mono relative time; header shows “N Classifications” + Edit/Export actions right; pagi-
nation “Page X of Y ·20 per page” pattern.
4. KPI latency/throughput: move into the analytics row as stat blocks with sparklines (live) — kill
the 4-identical-card row.
PHASE 2 — Detections page ←CrowdStrike Falcon pattern (reference #5,
structure only)
1. Severity filter rail (CrowdStrike’s left panel →top chips row): each chip = severity name +
REAL count (Critical 4, High 1, …), active state filters the table; include Status and Category chip
groups.
2. Expandable rows: click a row →inline expansion (height animated 120ms, reduced-motion safe)
showing: full subject (no truncation), confidence with all-6-class probability mini-bars (mono %),
source provenance (LIVE session / CACHED snapshot + timestamp), assigned/status, and a ML
explanation line (“Classified as Phishing by INT8 ONNX model ·confidence 0.92 ·processing 0.23
ms”) — all REAL data from the prediction/store, nothing invented.
Generated by Kimi.ai


--- Page 2 ---
3. Keep search/sort/pagination/CSV; keep per-row CACHED provenance.
PHASE 3 — Threat Analytics + System Health (references #4 and #1)
1. Threat Analytics: Sentinel pattern — full-width timeline (detections over time, area 6% max +
2px line, crosshair) with 4–6 category count blocks beneath (big mono number + category color
tick + name); below, the distribution bars + performance chart side by side.
2. System Health: Grafana stat-row density — probe status, latency sparkline (last 20 probes),
uptime, error count, requests/min; System Monitor tiles keep honest config values; footer shows
probe cadence.
GATES (all mandatory)
• build 0 errors, chunk <600KB ·lint 0/0 ·vitest green (update structural tests; lazy-chunk assertions
use findBy*) ·axe per view 0 ·contrast 23/23 AA.
• Honesty:
zero fabricated data;
live/ cached/ offline states truthful;
empty states per DE-
SIGN_BRIEF (never ghost charts).
• SIDE-BY-SIDE before/after screenshots: Dashboard, Detections (one row expanded), Threat An-
alytics, System Health at 1366px + rubric scores ￿9/10.
REPORT FORMAT
Per phase: composition changes (file:line + visible effect), what was copied from which reference and
what was mapped to tokens, side-by-side screenshot refs, rubric table, gates. Top line: “A Splunk ES
user opening this page would recognize: …”.
Generated by Kimi.ai

