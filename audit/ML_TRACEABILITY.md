# ML Traceability Matrix — Phase v4

This document traces every machine-learning claim, artifact, and UI number in
`mission/v4` back to a reproducible source. It is the ML-engineer sign-off for
Phase 3.

## Artifact Inventory

| Artifact | Path | Status | Generated By | SHA-256 (when recorded) |
|---|---|---|---|---|
| INT8 ONNX model | `model/artifact.onnx` | committed | `model/export_onnx.py` → `model/calibrate.py` | `ed10c4031405e3ab7e8767031a6c38d24d9c2f5075955ab08f1fdd2359a58713` |
| FP32 ONNX model | `model/artifact_fp32.onnx` | committed | `model/export_onnx.py` → `model/calibrate.py` | `701c7dece9ee1ece0580e5185b155dedacb362daf3b5499d5bd1aca550f8d6c1` |
| Trained sklearn pipeline | `model/pipeline.pkl` | committed | `model/train.py` | n/a (pickle) |
| Held-out test set | `model/test_set.jsonl` | committed | `model/train.py` | n/a |
| Evaluation results | `model/eval_results.json` | committed | `model/eval.py` | `05b4c580c9268dcd24ca01360c1e61531119c5f905e190b0e2ee0cad806c5bf0` |
| Confusion matrix | `model/confusion_matrix.json` | committed | `model/eval.py` | `545d09b7ea346f7f3d338e8484eba17842abc9ed2c61fc59d401598097a21da3` |
| Calibration report | `model/calibration.json` | committed | `model/calibrate.py` | `0b2c91e726065637c805d6fdc6f138cdcb751946f616d918d7e3eab3479f96f1` |
| Quantization report | `model/quantization.md` | committed | `model/export_onnx.py` | `d9425f3122adba02183189b39b3ab1d5f75bf04e9caf43aa158cd78570579d2d` |
| Local latency | `model/latency_local.json` | committed | `model/measure_latency.py` | `f48ec5876d1364dedc60ff5f79d74c989129baad47ffaf321dccdf3f8ad19122` |
| Graviton latency | `model/latency_t4g_micro.json` | committed | `model/measure_latency.py` | `835355a1c8410f589b190e1778b51b7c3ddce2064dcce7f9396e1e44c7144f81` |
| Probe results | `model/probe_results.json` | committed | `model/run_probe_suite.py` | `e69b92e321c616f12ce21bc8ca285ab36c96dc078ae5ec9e7ccd6872f7c97ce9` |
| Model meta | `model/artifact_meta.json` | committed | `model/calibrate.py` | `*` |
| Model card | `model/MODEL_CARD.md` | committed | hand-written, synced to artifacts | same as model |
| Cached snapshot | `public/cache/tickets-snapshot.json` | committed | local `/predict` responses | `ee2432b542e65a2e007b024b7074b5ff1d08f1758ec74a3a5fa9036c8ed47b72` |

`*` The SHA-256 of `model/artifact_meta.json` is intentionally not pinned here
because it is updated whenever the artifact changes; consumers should read its
`artifact_sha256` field.

## Claim → Evidence Mapping

| Claim | Evidence | Verification Command |
|---|---|---|
| 92.94% held-out accuracy | `model/eval_results.json` §`overall_accuracy` | `python -m model.eval` |
| 609 held-out samples | `model/eval_results.json` §`test_size` | `python -m model.eval` |
| GroupShuffleSplit by `seed_id` | `model/eval_results.json` §`methodology.split` | inspect `model/train.py` |
| All classes pass F1 ≥ 0.70 | `model/eval_results.json` §`per_class_metrics` | `python -m model.eval` |
| Winner vs. deployed candidate IDs | `model/eval_results.json` §`winner_candidate_id` / `deployed_candidate_id` | `python -m model.train` |
| 0.38 MB INT8 artifact size | `model/artifact.onnx` on disk + `model/artifact_meta.json` §`size_mb` | `ls -l model/artifact.onnx` |
| 700 MB memory budget | `ops/ticketsec.service` §`MemoryMax=700M` | inspect service file |
| 0.38 MB / 700 MB UI donut | `src/lib/artifacts.ts` → `model/artifact_meta.json` + `ops/ticketsec.service` | visual/UI inspection |
| Latency offline benchmark (p50/p95) | `src/lib/artifacts.ts` → `model/latency_t4g_micro.json` | inspect `ModelPerformancePanel` |
| Accuracy in Model Performance panel | `src/lib/artifacts.ts` → `model/eval_results.json` §`overall_accuracy` | inspect `ModelPerformancePanel` |
| Confusion matrix in UI | `src/lib/artifacts.ts` → `model/confusion_matrix.json` | inspect `ModelRegistry` |
| Quantization fidelity +0.0016 | `model/quantization.md` §Accuracy | `python -m model.export_onnx` |
| Calibration (ECE, Brier, temperature) | `model/calibration.json` §`ece_before/after`, `temperature` | `python -m model.calibrate` |
| No HTTP 5xx on probes | `model/probe_results.json` §`http_5xx_count` | `python -m model.run_probe_suite` |
| Local p50/p95 latency | `model/latency_local.json` | `python -m model.measure_latency` |

## Model Footprint Chain

```text
ops/ticketsec.service  MemoryMax=700M
         |
         v
model/artifact_meta.json  memory_max_mb=700, size_mb=0.38
         |
         v
src/lib/artifacts.ts  loads model/artifact_meta.json
         |
         v
src/components/ModelHealthDonut.tsx  renders size / budget donut
         |
         v
model/MODEL_CARD.md, model/quantization.md, UI labels
```

The UI donut reads the 0.38 MB and 700 MB figures from `src/lib/artifacts.ts`,
which loads `model/artifact_meta.json`. The memory budget is additionally
cross-checked against `ops/ticketsec.service`.

## Calibration Decision Log

- **Method:** Top-label Expected Calibration Error (ECE) with 10 equal-width bins
  and multiclass Brier score, computed from the INT8 ONNX probability outputs on
  the held-out test set. Temperature scaling was fit by grid search over `T` to
  minimize top-label ECE.
- **Result:**
  - ECE before = 0.3946, Brier before = 0.3194, assessment = `UNDERCONFIDENT`.
  - Optimal temperature `T = 0.271`.
  - ECE after = 0.0172, Brier after = 0.1089, assessment = `WELL_CALIBRATED`.
- **Interpretation:** The `class_weight="balanced"` logistic-regression pipeline
  was under-confident on the easy, synthetic dataset. Temperature scaling sharpens
  the probability distribution so that predicted confidence aligns with empirical
  accuracy. Because the dashboard uses argmax classification, the predicted
  category is unchanged; only the confidence/probability values are recalibrated.
- **Action:** The calibrated ONNX graph is committed as `model/artifact.onnx`.
  Live Graviton redeployment is pending host access.

## Sample Confidences Before/After

| Sample | Before | After |
|---|---:|---:|
| Phishing — "suspicious email asking for bank credentials" | 0.316 | 0.794 |
| Malware — "trojan horse detected in downloaded file" | 0.746 | 0.999 |
| Unauthorized Access — "multiple failed login attempts from unknown IP" | 0.520 | 0.989 |

See `model/calibration.json` §`sample_confidences` for the full per-class
probability vectors.

## Reproducibility Checklist

Run these commands in order to regenerate every artifact from source:

```bash
python -m model.train
python -m model.export_onnx
python -m model.eval
python -m model.calibrate
python -m model.eval

# With the backend running:
python -m model.measure_latency
python -m model.run_probe_suite
```

## Known Limitations

1. The dataset is synthetic and highly separable; metrics are demo-grade.
2. The winning ablation candidate (`C2_char3-5_word1-2_LR_C2.0`) cannot be
   exported to ONNX because `skl2onnx` does not support `analyzer="char_wb"`.
   The deployed artifact uses the best ONNX-exportable candidate.
3. Graviton latency numbers in `model/latency_t4g_micro.json` were refreshed
   during Phase 6 from the live AWS endpoint.
4. Calibration was fit on the held-out synthetic test set; real-world calibration
   may differ.

## Honesty Contract

Every datum shown is either live (from the API), cached (amber `CACHED` badge,
sourced from `public/cache/tickets-snapshot.json`), or shown as
"Unavailable — API offline". The Event Log records ONLY real events. Nothing is
ever fabricated and presented as live.
